/**
 * INFINOX Repeating Pattern Background
 * Yellow candlesticks + community network nodes drifting over IX Charcoal gradient.
 * Strictly follows INFINOX guidelines (#1E1E1E → #1D1D1B base, #FFCD05 graphic devices).
 */
const AnimatedBackground = () => (
  <div
    className="ix-pattern-bg fixed inset-0 z-0 pointer-events-none"
    aria-hidden="true"
  />
);

export default AnimatedBackground;
